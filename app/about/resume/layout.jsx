export const metadata = {
  title: "Resume",
  description: "Download Resume",
    keywords:"Resume, Kushagra Sharma Resume, tda resume, thedarkartist resume, the dark artist resume, developer reusme",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
