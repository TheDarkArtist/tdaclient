export const metadata = {
  title: "About",
  description: "It feels so empty without me.",
  metadataBase: new URL("https://thedarkartist.in/about"),
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
